VITE_WALLETCONNECT_PROJECT_ID=
VITE_FACTORY_ADDRESS=
VITE_LOCKER_ADDRESS=
VITE_LOCAL_RPC_URL=
VITE_INDEXER_URL=

# Server-only — deliberately NOT prefixed VITE_, so Vite never inlines it into
# the client bundle. Read exclusively by web/server/pin.ts. Unset in local dev
# falls back to MockPinProvider (no real IPFS pin, no credential needed).
PINATA_JWT=
